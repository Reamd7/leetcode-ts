import main from '../88.合并两个有序数组';

describe('合并两个有序数组', () => {
  it('给定 [1, 2, 3, 0, 0, 0], 3, [4, 5, 6], 3 => [1, 2, 3, 4, 5, 6]', () => {
    const a = [1, 2, 3, 0, 0, 0];
    main(a, 3, [4, 5, 6], 3);
    expect(
      a,
    ).toEqual([1, 2, 3, 4, 5, 6]);
  });
});
